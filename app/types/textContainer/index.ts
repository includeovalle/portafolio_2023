import { childrenInterface, H_Interface, classNamesInterface } from "@/app/types/";

export default interface Props extends childrenInterface, H_Interface, classNamesInterface { title?: React.ReactNode };
